apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
  name: cephfs-static-pi
  #namespace: CHANGEME
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 498T
  volumeMode: Filesystem
  volumeName: cephfs-static-pi-CHANGEME
