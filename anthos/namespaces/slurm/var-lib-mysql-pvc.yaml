apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: slurm-lib-mysql
  namespace: slurm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: csi-rbd-sc
