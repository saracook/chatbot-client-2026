apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: carina-login-duo-secret
spec:
  refreshInterval: 1h           # rate SecretManager pulls GCPSM
  secretStoreRef:
    kind: ClusterSecretStore
    name: gsm-store             # name of the SecretStore (or kind specified)
  target:
    name: carina-login-duo      # name of the k8s Secret to be created
    creationPolicy: Owner
  data:
  - secretKey: pam_duo.conf     # name of the GCPSM secret key
    remoteRef:
      key: carina-login-duo-secret
