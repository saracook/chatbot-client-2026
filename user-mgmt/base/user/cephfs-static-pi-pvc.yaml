apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cephfs-static-pi
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 498T
  volumeMode: Filesystem
  volumeName: cephfs-static-pi-default