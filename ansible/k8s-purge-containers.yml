- hosts: k8s
  become: yes
  gather_facts: no
  tasks:
    - ansible.builtin.shell:
        cmd: |
          for CID in $(crictl ps -a -q -s Exited)
          do
            crictl rm $CID
          done
          crictl rmi --prune
