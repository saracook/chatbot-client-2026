- name: install base os packages
  ansible.builtin.dnf:
    name: "{{ base_os_packages }}"
    state: present
    update_cache: yes

- name: install base os epel packages
  ansible.builtin.dnf:
    name: "{{ base_os_epel_packages }}"
    state: present
    update_cache: yes
    enablerepo: epel
