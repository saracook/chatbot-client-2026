- name: remove ansible ssh key directory
  hosts: localhost
  connection: local
  gather_facts: no
  tasks:
    - name: remove ansible ssh key directory
      ansible.builtin.file:
        path: "{{ ansible_ssh_private_key_file | dirname }}"
        state: absent
      changed_when: false
      tags:
        - always
